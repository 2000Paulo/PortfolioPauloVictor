{
  "name": "portfolio-paulo-victor",
  "version": "1.0.0",
  "scripts": {
    "build:css": "npx tailwindcss -i ./src/css/style.css -o ./src/css/style.css",
    "watch:css": "npx tailwindcss -i ./src/css/style.css -o ./src/css/style.css --watch",
    "build": "npm run build:css && echo \"Build completa\""
  },
  "devDependencies": {
    "autoprefixer": "^10.0.0",
    "postcss": "^8.0.0",
    "tailwindcss": "^3.0.0"
  }
}
